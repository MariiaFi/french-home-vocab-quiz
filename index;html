<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.5, user-scalable=yes">
    <title>Предметы дома — Французский</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div class="app-container">
        <!-- Верхняя панель навигации -->
        <header class="main-header">
            <div class="logo">
                <span class="logo-icon">🏠</span>
                <span>Français — Дом</span>
            </div>
            <div class="header-controls">
                <button id="themeToggle" class="icon-btn" aria-label="Сменить тему">🌙</button>
                <button id="backToDictBtn" class="icon-btn" title="Словарь">📚</button>
            </div>
        </header>

        <!-- Основное переключаемое содержимое -->
        <main id="appContent">
            <!-- Сюда React-подобно будет рендериться либо словарь, либо квиз -->
        </main>
    </div>

    <!-- Шаблоны для динамической вставки (не отображаются) -->
    <template id="dict-screen-template">
        <section class="screen dict-screen">
            <div class="search-sort-bar">
                <div class="search-wrapper">
                    <input type="text" id="searchInput" class="search-input" placeholder="Поиск по франц. или рус...">
                </div>
                <div class="sort-buttons">
                    <button id="sortAzBtn" class="sort-btn">A–Z</button>
                    <button id="sortRandomBtn" class="sort-btn">🎲</button>
                </div>
            </div>
            <div class="dict-header">
                <h2>Словарь (30 слов)</h2>
                <button id="startQuizFromDict" class="btn-primary">🧠 Квиз</button>
            </div>
            <div id="vocabGrid" class="vocab-grid">
                <!-- Карточки вставятся сюда -->
            </div>
        </section>
    </template>

    <template id="quiz-screen-template">
        <section class="screen quiz-screen">
            <div class="quiz-header">
                <h2 id="quizModeTitle">Квиз: Русский → Французский</h2>
                <button id="backToDictFromQuiz" class="btn-outline">← Словарь</button>
            </div>
            
            <!-- Прогресс бар -->
            <div class="progress-container">
                <div id="progressBar" class="progress-bar" style="width: 0%;"></div>
            </div>
            
            <!-- Счетчик правильных ответов -->
            <div class="score-counter" id="scoreCounter">Правильно: 0 / 0</div>

            <!-- Карточка вопроса (анимирована) -->
            <div class="quiz-card" id="quizCard">
                <div class="question-word" id="questionWord">загрузка...</div>
                <div class="options-container" id="optionsContainer">
                    <!-- Кнопки вариантов -->
                </div>
                <div class="quiz-footer">
                    <button id="nextBtn" class="btn-primary" disabled>Дальше</button>
                </div>
            </div>

            <!-- Экран результата (изначально скрыт) -->
            <div id="resultScreen" class="result-screen hidden">
                <div class="result-content">
                    <h3>🎉 Результат</h3>
                    <div class="result-stats" id="resultStats"></div>
                    <div class="result-actions">
                        <button id="retryMistakesBtn" class="btn-secondary">Повторить ошибочные</button>
                        <button id="playAgainBtn" class="btn-primary">Сыграть снова</button>
                        <button id="backToDictFromResult" class="btn-outline">В словарь</button>
                    </div>
                </div>
            </div>
        </section>
    </template>

    <script src="app.js"></script>
</body>
</html>
